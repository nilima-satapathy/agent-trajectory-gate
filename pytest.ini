[pytest]
testpaths = tests
pythonpath = .
markers =
    live: requires OPENAI_API_KEY and network (Groq / OpenAI-compatible)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
addopts = -ra
